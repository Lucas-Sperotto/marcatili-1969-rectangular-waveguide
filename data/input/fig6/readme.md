# Inventario de `data/input/fig6/`

Esta pasta guarda os arquivos de entrada dos paineis individuais da Fig. 6. Cada JSON define um painel ou subcaso usado por `reproduce_fig6`.

## Subpastas

Esta pasta nao possui subpastas.

## Arquivos

- `README.md`: nota preexistente com o agrupamento conceitual dos casos da Fig. 6 e observacoes sobre os limites de lamina e leituras de OCR.
- `readme.md`: este inventario detalhado da pasta `data/input/fig6/`.
- `SG-006a.json`: configuracao do painel `(a)` da Fig. 6 no conjunto de casos com guia retangular imerso e contraste fraco.
- `SG-006b.json`: configuracao do painel `(b)` da Fig. 6 no mesmo conjunto de guia imerso com contraste fraco.
- `SG-006c.json`: configuracao do painel `(c)` da Fig. 6 no mesmo conjunto de guia imerso com contraste fraco.
- `SG-006d.json`: configuracao do painel `(d)` da Fig. 6 no limite de lamina simetrica, tratado como problema unidimensional.
- `SG-006e.json`: configuracao do painel `(e)` da Fig. 6 no conjunto de casos com guia imerso e contraste mais forte.
- `SG-006f.json`: configuracao do painel `(f)` da Fig. 6 no mesmo conjunto de guia imerso com contraste mais forte.
- `SG-006g.json`: configuracao do painel `(g)` da Fig. 6 no mesmo conjunto de guia imerso com contraste mais forte.
- `SG-006h.json`: configuracao do painel `(h)` da Fig. 6 para o caso assimetrico do tipo Fig. 1a; `TODO`: revisar a leitura de OCR se a interpretacao das interfaces mudar.
- `SG-006i.json`: configuracao do painel `(i)` da Fig. 6 para o caso assimetrico do tipo Fig. 1a; `TODO`: revisar a leitura de OCR se a interpretacao das interfaces mudar.
- `SG-006j.json`: configuracao do painel `(j)` da Fig. 6 para o caso assimetrico do tipo Fig. 1a; `TODO`: revisar a leitura de OCR se a interpretacao das interfaces mudar.
- `SG-006k.json`: configuracao do painel `(k)` da Fig. 6 no limite de lamina assimetrica.
