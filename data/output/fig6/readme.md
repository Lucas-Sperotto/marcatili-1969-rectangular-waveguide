# Inventario de `data/output/fig6/`

Esta pasta concentra os artefatos gerados para cada painel da Fig. 6. Para cada caso `SG-006x`, o fluxo atual produz um `CSV`, um `JSON` e um `PNG`.

## Subpastas

Esta pasta nao possui subpastas.

## Arquivos

- `readme.md`: este inventario detalhado da pasta `data/output/fig6/`.
- `SG-006a.csv`: amostras numericas das curvas do painel `(a)` da Fig. 6.
- `SG-006a.json`: relatorio estruturado da execucao do painel `(a)` da Fig. 6.
- `SG-006a.png`: grafico final gerado para o painel `(a)` da Fig. 6.
- `SG-006b.csv`: amostras numericas das curvas do painel `(b)` da Fig. 6.
- `SG-006b.json`: relatorio estruturado da execucao do painel `(b)` da Fig. 6.
- `SG-006b.png`: grafico final gerado para o painel `(b)` da Fig. 6.
- `SG-006c.csv`: amostras numericas das curvas do painel `(c)` da Fig. 6.
- `SG-006c.json`: relatorio estruturado da execucao do painel `(c)` da Fig. 6.
- `SG-006c.png`: grafico final gerado para o painel `(c)` da Fig. 6.
- `SG-006d.csv`: amostras numericas das curvas do painel `(d)` da Fig. 6.
- `SG-006d.json`: relatorio estruturado da execucao do painel `(d)` da Fig. 6.
- `SG-006d.png`: grafico final gerado para o painel `(d)` da Fig. 6.
- `SG-006e.csv`: amostras numericas das curvas do painel `(e)` da Fig. 6.
- `SG-006e.json`: relatorio estruturado da execucao do painel `(e)` da Fig. 6.
- `SG-006e.png`: grafico final gerado para o painel `(e)` da Fig. 6.
- `SG-006f.csv`: amostras numericas das curvas do painel `(f)` da Fig. 6.
- `SG-006f.json`: relatorio estruturado da execucao do painel `(f)` da Fig. 6.
- `SG-006f.png`: grafico final gerado para o painel `(f)` da Fig. 6.
- `SG-006g.csv`: amostras numericas das curvas do painel `(g)` da Fig. 6.
- `SG-006g.json`: relatorio estruturado da execucao do painel `(g)` da Fig. 6.
- `SG-006g.png`: grafico final gerado para o painel `(g)` da Fig. 6.
- `SG-006h.csv`: amostras numericas das curvas do painel `(h)` da Fig. 6.
- `SG-006h.json`: relatorio estruturado da execucao do painel `(h)` da Fig. 6.
- `SG-006h.png`: grafico final gerado para o painel `(h)` da Fig. 6.
- `SG-006i.csv`: amostras numericas das curvas do painel `(i)` da Fig. 6.
- `SG-006i.json`: relatorio estruturado da execucao do painel `(i)` da Fig. 6.
- `SG-006i.png`: grafico final gerado para o painel `(i)` da Fig. 6.
- `SG-006j.csv`: amostras numericas das curvas do painel `(j)` da Fig. 6.
- `SG-006j.json`: relatorio estruturado da execucao do painel `(j)` da Fig. 6.
- `SG-006j.png`: grafico final gerado para o painel `(j)` da Fig. 6.
- `SG-006k.csv`: amostras numericas das curvas do painel `(k)` da Fig. 6.
- `SG-006k.json`: relatorio estruturado da execucao do painel `(k)` da Fig. 6.
- `SG-006k.png`: grafico final gerado para o painel `(k)` da Fig. 6.
